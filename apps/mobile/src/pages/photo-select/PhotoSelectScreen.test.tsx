/**
 * 동작 범위 (2026-07-30 인터뷰)
 *
 * 진입하면 선택된 앨범에서 100개 분석 단위를 불러와 전체 선택 상태로 시작한다.
 * 타일 누름은 대상에 따라 다르게 동작한다 — 단일 사진은 제외, 그룹 대표는 다음 사진으로 승계.
 * 제외 개수에 상한은 없고, 90 미만이면 CTA만 비활성화한다(PRD의 "최대 10개"는 제출 가능 범위 90~100을 뜻함).
 *
 * 제외: 진입 시 갤러리 전체에 100개 미만 → 생성 불가 안내 화면 — 별도 작업, Unable 시안 없음
 * 제외: 권한 요청·거부 흐름 — 앱 진입에서 처리
 * 제외: 백그라운드 중 설정에서 권한 회수 후 복귀 — 드묾, 실제 문제 시 추가
 * 제외: CTA의 분석 생성 API 연동 — 이번엔 라우팅만, useCreateAnalysisMutation 연결은 다음 작업
 * 제외: 로딩 중 타일 표현 — 시안의 회색 타일은 샘플 필러이지 플레이스홀더가 아님
 *
 * [팀확인] 700:7641 시안의 dim 누락 — 디자이너 확인, 구현은 dim 적용
 */

describe('진입', () => {
  it.todo('선택된 앨범에서 불러온 100개를 전체 선택 상태로 표시한다');
  it.todo('그룹 대표와 단일 사진만 그리드에 표시한다');
});

describe('단일 사진 제외', () => {
  it.todo('타일을 누르면 제외되고 카운터가 1 줄어든다');
  it.todo('제외된 타일은 체크 해제 상태로 표시된다');
  it.todo('제외된 타일을 다시 누르면 복구되고 카운터가 1 늘어난다');
});

describe('그룹 대표 승계', () => {
  it.todo('그룹 대표를 누르면 다음 사진이 대표가 되고 카운터는 그대로다');
  it.todo('마지막 사진까지 누르면 그룹이 제외되고 카운터가 1 줄어든다');
  it.todo('제외된 그룹을 다시 누르면 첫 대표로 복구되고 카운터가 1 늘어난다');
});

describe('카운터와 CTA', () => {
  it.todo('현재 분석 단위 수를 100 기준으로 표시한다');
  it.todo('90 미만이면 카운터를 경고 색으로 표시한다');
  it.todo('90 미만이면 CTA를 비활성화한다');
  it.todo('90 이상으로 회복하면 CTA가 다시 활성화된다');
  it.todo('제외 개수에 상한이 없어 90 밑으로 내려갈 수 있다');
  it.todo('CTA를 누르면 다음 화면으로 이동한다');
});

describe('전체 취소와 자동 선택', () => {
  it.todo('전부 선택된 상태면 전체 취소를 표시한다');
  it.todo('하나라도 제외됐으면 자동 선택을 표시한다');
  it.todo('전체 취소를 누르면 전부 제외되어 0이 된다');
  it.todo('자동 선택을 누르면 불러온 100개를 전부 선택한다');
});

describe('앨범 전환', () => {
  it.todo('앨범을 고르면 그 앨범에서 100개를 새로 불러온다');
  it.todo('앨범을 바꾸면 이전 앨범의 제외가 버려지고 전체 선택으로 초기화된다');
  it.todo('앨범에 100개가 안 되면 있는 만큼 불러오고 CTA를 비활성화한다');
});
