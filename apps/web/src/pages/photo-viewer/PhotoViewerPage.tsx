type PhotoViewerPageProps = {
  stickerId: string;
  initialIndex: string;
};

export function PhotoViewerPage({
  stickerId: _stickerId,
  initialIndex: _initialIndex,
}: PhotoViewerPageProps) {
  return <div>사진 크게 보기</div>;
}
